# =============================================================================
# Supabase Configuration
# =============================================================================
# Get these values from your Supabase project settings (https://supabase.com/dashboard)
# Project Settings > API

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url

# Supabase anonymous/public key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key (NEVER expose to client, server-side only)
# This key bypasses Row Level Security - use with caution
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# NextAuth Configuration
# =============================================================================
# NextAuth base URL (use http://localhost:3000 for development)
NEXTAUTH_URL=http://localhost:3000

# NextAuth secret for JWT encryption
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-generated-secret-here

# =============================================================================
# Application Configuration
# =============================================================================
# Base URL for the application (used for QR code generation)
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Application name (displayed in UI)
NEXT_PUBLIC_APP_NAME=Attendance Location for Groups

# =============================================================================
# Development Notes
# =============================================================================
# 1. Copy this file to .env.local and fill in actual values
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. For production deployment on Vercel:
#    - Add all NEXT_PUBLIC_* variables as environment variables
#    - Mark NEXT_PUBLIC_* as "Exposed to client"
#    - Keep server-side keys (SUPABASE_SERVICE_ROLE_KEY, NEXTAUTH_SECRET) unexposed
# 4. Update NEXT_PUBLIC_BASE_URL to your production domain when deploying
# 5. Do not use or publish default credentials. Create organizations via /register.
